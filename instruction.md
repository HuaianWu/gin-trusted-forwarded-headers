为部署在多级反向代理后的 Gin 应用增加对 RFC 7239 Forwarded 请求头的可选可信解析，使处理器能够从同一条经过验证的代理链获得客户端 IP、外部协议和外部主机信息；该能力默认关闭，开启后只在直连对端及链路中相应代理符合现有受信代理配置时采用完整 Forwarded 元素，并正确处理标准允许的多值字段、引号转义和地址表示。遇到无法识别的节点、非法语法、无效协议或主机以及无法建立完整信任链时，不得接受可伪造的部分结果，应回退到现有 RemoteAddr 与 X-Forwarded-* 兼容逻辑；真实 TLS 连接仍优先报告 https，现有 ClientIP、Scheme 和未启用时的行为不得改变。
